name: Create Release

on:
  push:
    tags:
      - "*" # triggers on any tag push

jobs:
  release:
    runs-on: codeberg-small
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y zip python3 python3-yaml

      - name: Install Nix
        run: |
          curl -L https://nixos.org/nix/install | sh -s -- --daemon
          . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
          nix --version

      - name: Install git-cliff
        run: |
          curl -L "https://github.com/orhun/git-cliff/releases/download/v2.12.0/git-cliff-2.12.0-x86_64-unknown-linux-gnu.tar.gz" -o git-cliff.tar.gz
          tar -xzf git-cliff.tar.gz --strip-components=1
          chmod +x git-cliff
          mv git-cliff /usr/local/bin/
          git-cliff --version

      - name: Get latest tag
        id: get_tag
        run: |
          TAG=$(git describe --tags --abbrev=0)
          echo "Latest tag: $TAG"
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Create zip files for themes
        run: |
          THEMES=(mocha macchiato frappe latte)
          for theme in "${THEMES[@]}"; do
            zip -r "${theme}.zip" "themes/$theme"
          done

      - name: Generate changelog
        id: changelog
        run: |
          git fetch --tags
          LATEST_TAG=$(git describe --tags --abbrev=0)
          PREV_TAG=$(git describe --tags --abbrev=0 $LATEST_TAG^)
          git-cliff --tag "$PREV_TAG..$LATEST_TAG" > changelog.md
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          cat changelog.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create release
        env:
          FJ_API_TOKEN: ${{ secrets.FORGEJO_TOKEN }}
        run: |
          TAG="${{ steps.get_tag.outputs.tag }}"
          CHANGELOG_FILE="${TAG}-changelog.md"
          echo "${{ steps.changelog.outputs.changelog }}" > "$CHANGELOG_FILE"
          . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
          nix --extra-experimental-features "nix-command flakes" shell nixpkgs#tea -c \
            tea release create \
              --remote "origin" \
              --repo "scarcekoi/pride-flags" \
              --tag "$TAG" \
              --title "$TAG" \
              --note-file "$CHANGELOG_FILE" \
              --asset "mocha.zip" \
              --asset "macchiato.zip" \
              --asset "frappe.zip" \
              --asset "latte.zip" \
              --asset flag_zips/*.zip
